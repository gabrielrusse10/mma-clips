{
  "name": "mma-clips",
  "version": "1.0.0",
  "private": true,
  "description": "Recent MMA highlights from the official promotion channels, rebuilt daily.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node scripts/serve.mjs",
    "update": "node scripts/update.mjs",
    "doctor": "node scripts/doctor.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
